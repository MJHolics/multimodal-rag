# GraphRAG 스킬업 — 진행 노트 (2026-07-27 → 07-29 갱신)

> 타겟: **그래파이(★★★★★)·휴머스온·에이치시스템즈** 공통 갭 = "Graph DB(Neo4j 등) 무경험".
> 접근: 새 프로젝트를 만들지 않고 **TechDocRAG에 그래프 검색 경로를 얹어 기존 하이브리드와 실측 비교**.

---

# ⚠️ 2026-07-29 갱신 — 1차 결론이 뒤집혔다

아래 1차 결과(청크 15)는 **코퍼스가 너무 쉬웠다.** 스스로 "top-3가 전체의 20%라 관대하다"고
적어두고 보류했던 그 문제가 실제로 결론을 만들고 있었다. 코퍼스를 51청크로 늘리고
**distractor**(엔티티는 언급하나 답은 아닌 청크 — 안전 주의사항·정비 절차·구형 사양·용어집)를
넣자 결과가 이렇게 바뀐다.

## 코퍼스 난이도별 (top-3, full_hit, dense=BAAI/bge-m3, 질의 16)

| 코퍼스 | hybrid_flat 전체/멀티홉 | fused(mention, w=0.5) 전체/멀티홉 |
|---|---:|---:|
| 1차 15청크 (top-3 = 20%) | 0.812 / 0.625 | **0.875 / 0.750** |
| 확장 27청크, distractor 없음 | 0.812 / 0.625 | **0.875 / 0.750** |
| **확장 51청크 + distractor (top-3 = 5.9%)** | **0.688 / 0.375** | **0.625 / 0.250** ← 융합이 더 나쁨 |

`graph_weight`를 0.0~1.0으로 스윕해도 **그래프를 섞어 플랫을 이기는 지점이 없다**(최고 0.688 = w=0 동률).
즉 1차의 "융합이 둘 다 이긴다 / 그래프는 보완재"는 **쉬운 코퍼스가 만든 결론**이었다.
(도메인만 늘린 27청크에서는 수치가 1차와 완전히 동일 — 즉 코퍼스 *크기*가 아니라 **distractor의
존재**가 변수였다. 크기만 키우고 헷갈리는 문서를 안 넣으면 아무것도 검증되지 않는다.)

## 원인 — 내 그래프 점수 함수의 결함

```python
closest = min(dist[e] for e in c.entities if e in dist)
score = 1.0 / (1.0 + closest) + 0.01 * len(ds)
```

이 점수는 *관계를 서술하는가*가 아니라 **엔티티를 언급하는가**를 잰다.
"고전압 배터리팩 취급 시 절연 장갑을 착용한다"는 시드 엔티티를 직접 언급하므로 거리 0 →
점수 1.0으로 **정답 청크와 동점**이 되고, 동점은 chunk_id 사전순으로 끊기니 운에 맡겨진다.
1차 코퍼스는 *모든 청크가 관계를 하나씩* 갖고 있어서 "엔티티 언급 = 관계 서술"이 우연히
성립했고, 그래서 이 결함이 드러나지 않았다.

## 수정 — 관계 인식 점수(`GraphRetriever(mode="relation")`)

확장 집합 안에서 **양 끝점이 모두 닿는 관계**만 근거로 인정한다. 관계를 하나도 서술하지 않는
청크(=distractor)는 점수가 0이 된다.

| 전략 (51청크 + distractor) | 전체 | 단일홉 | 멀티홉 | MRR(전체) |
|---|---:|---:|---:|---:|
| hybrid_flat | 0.688 | 1.000 | 0.375 | 0.833 |
| graph_linked (mention, 기존) | 0.438 | 0.625 | 0.250 | 0.594 |
| **graph_rel (관계 인식)** | **0.812** | 1.000 | **0.625** | 0.823 |
| fused (mention, w=0.5) | 0.625 | 1.000 | 0.250 | 0.875 |
| **fused_rel (w=0.5)** | **0.812** | 1.000 | **0.625** | **0.927** |

`graph_weight` 스윕(relation): w≥0.5에서 0.812/0.625로 평평 → **w=0.5 유지가 타당**(1차의 눈대중
0.5를 실측이 사후 정당화한 셈. mention 모드였다면 0.0이 최적이었다).

## 유의성 — 주장할 수 있는 것과 없는 것 (McNemar 정확검정, 질의 16개 짝지음)

| 비교 | 불일치 쌍 | p | 판정 |
|---|---:|---:|---|
| hybrid_flat → graph_rel (0.688→0.812) | 1 : 3 | 0.63 | **유의하지 않음** |
| graph_linked → graph_rel (0.438→0.812) | 0 : 6 | **0.031** | **유의** |
| fused → fused_rel (0.625→0.812) | 0 : 3 | 0.25 | 유의하지 않음 |
| graph_rel vs fused_rel | 0 : 0 | 1.00 | 커버리지 동일(MRR만 0.823→0.927) |

**그래서 정직한 결론은 이것이다:**
1. **"그래프가 플랫 하이브리드보다 낫다"는 이 평가셋으로 주장할 수 없다** — 질의 16개로는
   0.688 vs 0.812가 불일치 4쌍에 불과해 검정력이 없다.
2. 주장할 수 있는 건 **"내 점수 함수의 결함을 찾아 고친 것이 유의하다"**(p=0.031)는 쪽이다.
3. 융합은 커버리지(full_hit)를 바꾸지 않고 **순위 품질만 올린다**(MRR 0.823 → 0.927).
   즉 그래프가 정답 집합을 찾고, 어휘 검색이 그 안의 순서를 정리한다.
4. **relation 모드가 공짜로 얻는 것을 명시해야 한다** — 이 점수는 "청크 → 관계 추출(IE)"이
   이미 정확하다고 가정하고, 합성 코퍼스는 그걸 공짜로 준다. 실문서라면 IE 품질이 그대로
   상한이 된다. 특히 구형 사양 distractor는 실제 IE라면 "(구형 팩, 전압, 350V)"를 뽑아낼
   텐데 여기서는 그런 노드를 그래프에 안 넣어서 자동으로 걸러진다.

## 다음 (우선순위 갱신)
1. **평가셋 확대가 최우선** — 위 1번이 곧 이 항목이다. 질의 16개로는 무엇도 결론 낼 수 없다.
   그래프에서 2-hop 경로를 열거해 40~60문항으로 늘리면 검정력이 생긴다.
2. **Neo4j 실행 검증** — 여전히 미완(아래 'Graph DB 상태' 참조). 갭을 실제로 닫는 유일한 항목.
3. 실 PDF 적용 — 합성 코퍼스는 엔티티 표기가 일관돼 링킹이 실제보다 쉽다.

---

# 1차 기록 (2026-07-27) — 위 갱신으로 무효화된 결론 포함, 이력으로 남긴다

## 실측 결과 (top-3, dense=BAAI/bge-m3, 청크 15 · 질의 16)

주 지표는 `full_hit` — **정답을 구성하는 청크가 전부** 상위 k에 들어야 1점.
멀티홉은 근거가 다 모여야 답이 생성되므로, "하나만 맞아도 hit"인 기존 정의로는 차이가 지워진다.

| 전략 | 전체 | 단일홉 | **멀티홉** |
|---|---:|---:|---:|
| hybrid_flat (현행: dense 0.7 + BM25 0.3) | 0.812 | 1.000 | 0.625 |
| graph_given (그래프 단독, 시드 1개) | 0.750 | 1.000 | 0.500 |
| graph_linked (그래프 단독, 엔티티 링킹) | 0.812 | 1.000 | 0.625 |
| **fused (하이브리드 + 그래프 0.5)** | **0.875** | 1.000 | **0.750** |

### 결론 (정직하게) — ⚠️ 2·3번은 2026-07-29 재실험으로 **철회**됨(위 갱신 참조)
1. **그래프 단독은 현행 하이브리드를 못 이긴다** — 멀티홉에서 0.625로 동률.
2. ~~**융합이 둘 다 이긴다**~~ — 멀티홉 0.625 → 0.750, 전체 0.812 → 0.875.
   ~~그래프의 가치는 대체재가 아니라 보완재다.~~
   → **철회.** distractor를 넣자 융합(mention)이 플랫보다 *나빠졌다*(0.625 vs 0.688).
   보완재라는 결론 자체는 관계 인식 점수로 고친 뒤에야 성립하고, 그것도 유의하진 않다.
3. 단일홉은 세 전략 모두 1.000 — 예상대로 플랫 검색으로 충분하고 그래프는 이득이 없다.
   "GraphRAG가 낫다"를 뭉뚱그리면 안 되는 이유.
4. dense 임베딩이 붙자 플랫 하이브리드의 멀티홉이 크게 올랐다(BM25 단독 0.375 → 하이브리드 0.625).
   그래프의 상대 이점 상당 부분을 dense가 이미 흡수한다.

## 실험 도중 잡은 설계 결함 (⑥ 에러 대처)
**1차 평가셋이 무효였다.** 멀티홉 질문이 다리(bridge) 엔티티를 그대로 노출해서
(예: "셀 모듈을 포함하는 **배터리팩**은 어느 공급사에서?") 두 gold 청크가 같은 단어를 공유했고,
플랫 BM25가 그냥 둘 다 찾았다 → 멀티홉이 아니라 키워드 검색 문제였다.
질문을 다시 쓰고(q09·q10·q14) **불변식 테스트로 고정**했다:
`test_multihop_questions_do_not_leak_the_bridge_entity`.

**명명도 하나 틀렸다.** `given` 시드를 처음엔 oracle(상한)이라 불렀는데, 실측에서 `linked`가 더 높았다
— 질문에 엔티티가 여럿이면 링킹이 앵커를 더 많이 찾기 때문. "정답 시드 vs 추정 시드"가 아니라
**"단일 앵커 vs 다중 앵커"** 차이다. 주석을 고쳤다.

## Graph DB 상태 (정직한 범위)
- ✅ **Kuzu(임베디드 Graph DB)로 Cypher 탐색 실행·검증.** in-memory 순수 탐색과 **결과 동일**함을
  테스트로 고정(`test_kuzu_cypher_matches_in_memory`). 서버·Docker 불필요.
- ⚠️ **Neo4j는 코드만 있고 실행 못 했다** — 이 개발 환경에 Docker가 없다(확인함).
  `Neo4jGraphStore`는 구현돼 있고 서버가 있으면 동작하도록 짰으나, **돌린 적 없다고 명시할 것.**
  → 다음 세션 과제: Neo4j AuraDB 무료 티어(가입 필요) 또는 Docker 설치 후 실행 검증.

## 파일
```
graph_rag/
  corpus.py       합성 기술문서 + ground-truth 그래프(결정적)
  eval_set.py     단일홉 8 + 멀티홉 8 질의, gold는 그래프에서 유도
  metrics.py      hit / full_hit / MRR / recall (full_hit이 주지표)
  graph_store.py  InMemory · Kuzu(Cypher, 검증됨) · Neo4j(미실행)
  retrieval.py    HybridRetriever · GraphRetriever · FusedRetriever
run_graph_eval.py 실험 러너 → output/graph_eval.json
tests/test_graph_rag.py  16 passed
```

## 다음에 이어서 할 일 (우선순위)
1. **graph_weight 스윕** — 지금 0.5 고정. 0.1~0.9로 재서 최적값을 실측으로 정할 것
   (하이브리드 70/30을 실측으로 정한 것과 같은 방식). 이게 ⑤ 교체 기록이 된다.
2. **코퍼스 확대 + distractor 추가** — 현재 청크 15개는 top-3가 전체의 20%라 너무 관대하다.
   엔티티/문서를 늘려 경쟁을 만들고 수치를 다시 낼 것. **현재 숫자는 소규모 통제 실험임을 명시.**
3. **Neo4j 실행 검증**(위 참조).
4. 실 PDF 적용 — 합성은 엔티티 표기가 일관돼 그래프 구축이 실제보다 쉽다.
   실 문서에선 별칭·약어 정규화가 추가 난이도. 지금 수치는 **GraphRAG에 유리한 상한**으로 읽어야 함.
5. 결과가 굳으면 → 프로젝트 카드 `techdocrag-multimodal.md` ④/⑤/⑥ 갱신 → 그래파이·휴머스온 이력서에
   "Graph DB(Cypher)" 반영 + 갭 해소 문구로 교체.

## 이력서/카드 반영 상태 (2026-07-29 갱신)

1차 보류 사유였던 "코퍼스 확대"는 끝났다. 다만 **쓸 수 있는 문장이 바뀌었다.**

**쓰면 안 되는 문장** — "GraphRAG로 멀티홉 검색을 0.625→0.750으로 올렸다"
(쉬운 코퍼스가 만든 수치이고, 어렵게 만들면 재현되지 않으며, 유의하지도 않다.)

**쓸 수 있는 문장** — 결과가 아니라 **실험 설계와 자기 수정**이 자산이다:
- 그래프 검색을 붙여 기존 하이브리드와 head-to-head로 비교하고, 단일홉/멀티홉을 분리해 쟀다.
- 처음엔 그래프 융합이 이겼는데, 코퍼스에 distractor를 넣자 **오히려 나빠졌다.** 원인이
  "내 점수 함수가 관계가 아니라 엔티티 언급을 재고 있었다"임을 찾아 고쳤고(0.438→0.812,
  McNemar p=0.031), 그 수정만이 유의한 개선이라는 것까지 검정으로 확인했다.
- "그래프가 낫다"는 주장은 이 평가셋(16문항)으로 **주장할 수 없다**고 명시한다.

즉 GraphAI·휴머스온 면접에서 꺼낼 것은 "그래프가 좋습니다"가 아니라
**"그래프가 언제 이득이고 언제 아닌지, 그리고 그걸 어떻게 재야 속지 않는지"**다.

여전히 **Neo4j 실행은 미완**이므로 "Neo4j 경험 있음"으로 쓸 수 없다. Kuzu(Cypher) 검증까지가 사실.
