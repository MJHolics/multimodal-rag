# Hugging Face Space 전용 최소 의존성.
#
# 본 프로젝트의 requirements.txt는 PDF 인제스천(PyMuPDF·pdfplumber)·ChromaDB·Ollama·Streamlit을
# 포함하는데, 데모는 그 어느 것도 쓰지 않는다. 무료 CPU Space는 빌드가 느려서 안 쓰는 의존성을
# 넣으면 콜드 스타트만 길어진다.
#
# **sentence-transformers를 일부러 뺐다.** torch까지 끌고 오면 빌드가 수 분 길어지고 무료 CPU
# (16GB)에서 BGE-M3(2GB+) 상주가 부담이다. 대신:
#   - 예제 질의 → demo_assets에 실은 사전 계산 BGE-M3 임베딩으로 즉시 응답(품질 동일)
#   - 자유 질의 → BM25 + 그래프로 검색하고 그 사실을 화면에 표시
# 빌드가 실패한 Space가 조금 느린 Space보다 훨씬 나쁘다는 판단이다.
# 로컬에서는 requirements.txt로 sentence-transformers가 설치되므로 자유 질의도 dense로 돈다.
#
# gradio는 Space가 sdk 설정으로 직접 설치하므로 여기 적지 않는다(버전 충돌 방지).
numpy>=1.26.0
rank-bm25>=0.2.2
